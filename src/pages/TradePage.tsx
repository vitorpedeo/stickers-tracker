import { AppFrame } from '../components/AppFrame'

export function TradePage() {
  return (
    <AppFrame>
      <section className="panel">
        <h1 className="page-title">Trade</h1>
        <p className="page-subtitle">This page is no longer part of the primary app flow.</p>
      </section>
    </AppFrame>
  )
}
